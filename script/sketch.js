function setup() {
    colorMode(RGB, 255);
    createCanvas(width, height);
}

function draw() {
    background(220);
    colorMode(RGB, 255, 255, 255, 1);

    oreils()
    visage()
    bouche()
    nez()
    yeux()
}